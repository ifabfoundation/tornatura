# Il resolve va dichiarato anche qui: l'inferenza delle dipendenze di Pants
# considera solo i target con resolve compatibile, quindi senza questa riga i
# moduli non entrerebbero nel pex e l'API fallirebbe all'import.
python_sources(
    resolve="landscape"
)
